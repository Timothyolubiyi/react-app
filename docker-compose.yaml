version: '3.3'
services:
  react_app:
    build: .
    image: smartgigsctf/football
    restart: unless-stopped
    privileged: true
    user: root
    ports:
      - 3000:3000
    container_name: react-container